name: GitHub Actions
on:
  push:
    branches:
      - master
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    container:
      image: webdeveloppro/tabs-yarn:latest   # or ghcr.io/my-org/my-app:1.2.3
    env:
      APP_DIR: /app/apps/advayta-org
      GIT_DIFF_FILE: /app/git_diff.txt
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true  # Fetch Hugo themes (true OR recursive)
          fetch-depth: 0    # Fetch all history for .GitInfo and .Lastmod

      # Why do we need it?
      # - name: Build and push
      #   uses: docker/build-push-action@v4
      #   with:
      #     push: true
      #     context: .
      #     tags: |
      #       cr.webdevelop.pro/webdevelop-pro/invest.webdevelop.pro:latest
     
      - name: Find required build
        run: |
          set -a
          . "$APP_DIR/.master.env"
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          ## fix russian letters
          export LANG=en_US.UTF-8
          export LC_ALL=en_US.UTF-8
          git config --global core.quotepath false
          
          git diff-tree --no-commit-id --name-status -r HEAD > "$GIT_DIFF_FILE"
          cat "$GIT_DIFF_FILE"
          rm -rf "$APP_DIR/docs"
          cp -rf "$GITHUB_WORKSPACE" "$APP_DIR/docs"
          cd "$APP_DIR"
          chmod +x ./make.sh
          export VITE_SRC_DIR=$GITHUB_WORKSPACE
          

          ## run build to identify last changes
          node etc/trigger_builds.cjs
          echo "home" >> updated_stages.txt
          cat updated_stages.txt

      - name: Deploy
        run: |
          cd "$APP_DIR"
          set -a
          . .master.env
          ./make.sh deploy-needed
          echo "done"

      - name: Update algolia search
        run: |
          cd "$APP_DIR"
          set -a
          . .master.env
          ls "$APP_DIR/docs"
          # Helper: returns 0 if the path should be excluded (.github / .obsidian / non-md).
          should_skip() {
            p="$1"
            case "$p" in
              ""|*".github"*|*".obsidian"*) return 0 ;;
              *.md) return 1 ;;
              *) return 0 ;;
            esac
          }

          if [ -s "$GIT_DIFF_FILE" ]; then
              # git diff-tree --name-status output: "<STATUS>\t<path>" or "R100\t<old>\t<new>"
              while IFS= read -r line || [ -n "$line" ]; do
                if [ -z "$line" ]; then continue; fi

                status=$(printf '%s' "$line" | cut -f1)
                case "$status" in
                  A|M|T)
                    file=$(printf '%s' "$line" | cut -f2)
                    if should_skip "$file"; then
                      echo "Skipping: $file"
                      continue
                    fi
                    echo "Updating: docs/$file"
                    node search/index.mjs --update "$file" || echo "Update failed (continuing): $file"
                    ;;
                  D)
                    file=$(printf '%s' "$line" | cut -f2)
                    if should_skip "$file"; then
                      echo "Skipping deleted: $file"
                      continue
                    fi
                    echo "Deleting from algolia: $file"
                    node search/index.mjs --delete "$file" || echo "Delete failed (continuing): $file"
                    ;;
                  R*|C*)
                    old=$(printf '%s' "$line" | cut -f2)
                    new=$(printf '%s' "$line" | cut -f3)
                    if should_skip "$old"; then
                      echo "Skipping renamed-from: $old"
                    else
                      echo "Deleting renamed-from: $old"
                      node search/index.mjs --delete "$old" || echo "Delete failed (continuing): $old"
                    fi
                    if should_skip "$new"; then
                      echo "Skipping renamed-to: $new"
                    else
                      echo "Updating renamed-to: docs/$new"
                      node search/index.mjs --update "$new" || echo "Update failed (continuing): $new"
                    fi
                    ;;
                  *)
                    echo "Skipping unknown status '$status': $line"
                    ;;
                esac
              done < "$GIT_DIFF_FILE"
          else
              echo "File is empty, skipping..."
          fi
          
          echo "done"

      - name: Purge cache
        run: |
          cd "$APP_DIR"
          set -a
          . .master.env
          set +a

          : "${CLOUDFLARE_ZONE_ID:?CLOUDFLARE_ZONE_ID is required}"
          : "${CLOUDFLARE_API_KEY:?CLOUDFLARE_API_KEY is required}"

          curl --fail --silent --show-error \
            -X POST "https://api.cloudflare.com/client/v4/zones/$CLOUDFLARE_ZONE_ID/purge_cache" \
            -H "Authorization: Bearer $CLOUDFLARE_API_KEY" \
            -H "Content-Type: application/json" \
            --data '{"purge_everything":true}'

      # ToDO: Change to Deploy All
      # - name: Deploy
      #   uses: cloudflare/wrangler-action@v3
      #   with:
      #     apiToken: ${{ secrets.CF_API_TOKEN }}
      #     accountId: ${{ secrets.CF_ACCOUNT_ID }}
      #     command: pages deploy .vitepress/dist --project-name="invest-webdevelop-biz" --commit-dirty true --branch main --commit-hash "${{ github.sha }}" --commit-message "${{ github.sha }}"

      # ToDo
      # Add variables
      # - name: Purge cache
      #   uses: jakejarvis/cloudflare-purge-action@master
      #   env:
      #     CLOUDFLARE_ZONE: ${{ secrets.CLOUDFLARE_ZONE }}
      #     CLOUDFLARE_TOKEN: ${{ secrets.CLOUDFLARE_TOKEN }}

      # - name: Slack Notification
      #   if: always() && ( github.ref == 'refs/heads/master' || github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/stage')
      #   uses: rtCamp/action-slack-notify@v2
      #   env:
      #     # SLACK_CHANNEL: invest-webdevelop-pro # it's not working, for change channel create another webhook for mew channel https://api.slack.com/apps/A044SB57YLD/incoming-webhooks
      #     SLACK_COLOR: ${{ job.status }}
      #     SLACK_TITLE: ${{ github.event.repository.name }}
      #     SLACK_MESSAGE: ${{ github.event.head_commit.message }}
      #     SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
